spring:
  profiles:
    active: test

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.tszj.chain.sdk.entities.pojo

logging:
  file: logs/sdk.log
  level:
    org.apache.http: info
    com.tszj.chain.gmc4j: info
    com.tszj.chain.sdk: info

